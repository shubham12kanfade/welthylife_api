import * as debug from 'debug';
import { AuthnRequestResponse } from './verify-service-provider-api/authn-request-response';
import { TranslatedMatchingResponseBody, TranslatedIdentityResponseBody } from './verify-service-provider-api/translated-response-body';
import { ErrorMessage } from './verify-service-provider-api/error-message';
export default class VerifyServiceProviderClient {
    private verifyServiceProviderHost;
    infoLog: debug.Debugger;
    requestLog: debug.Debugger;
    constructor(verifyServiceProviderHost: string);
    generateAuthnRequest(levelOfAssurance: ('LEVEL_1' | 'LEVEL_2'), entityId?: string): Promise<{
        status: number;
        body: AuthnRequestResponse | ErrorMessage;
    }>;
    translateResponse(samlResponse: string, requestId: string, levelOfAssurance: ('LEVEL_1' | 'LEVEL_2'), entityId?: string): Promise<{
        status: number;
        body: TranslatedMatchingResponseBody | TranslatedIdentityResponseBody | ErrorMessage;
    }>;
    private sendRequest;
}
